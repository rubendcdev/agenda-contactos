# Agenda de Contactos (Flask)

Aplicación de agenda de contactos en memoria usando Flask.

## Estructura
- `app/` → contiene `app.py` con toda la lógica
- `tests/` → pruebas con pytest
- `.github/workflows/` → pipeline CI

## Ejecutar la app
```bash
python -m app.app
